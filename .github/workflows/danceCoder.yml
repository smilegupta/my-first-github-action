name: Dance Robot

on: push

jobs:
  dance-robot:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run Dance Robot
        run: |
          node dancing-robot.js
